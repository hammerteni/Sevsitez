<?xml version="1.0" encoding="utf-8"?>
<configuration>
    <system.web>
      <httpRuntime executionTimeout="600" maxRequestLength="30000" requestValidationMode="2.0" />
    </system.web>
  <system.webServer>
    <defaultDocument>
      <files>
        <!-- очищаем содержимое в "Документ по умолчанию" -->
        <clear/>
        <!-- добавляем нужную нам страницу запуска в "Документ по умолчанию" -->
        <add value="as.aspx"/>
      </files>
    </defaultDocument>
  </system.webServer>
</configuration>
